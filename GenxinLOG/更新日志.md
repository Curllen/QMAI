# 更新日志

## 20260621-0006

1. 修复拆书库点击“已提取角色”后进行深度 6 维/简单提取时，因未传入章节选择导致“未能读取到任何章节内容”的问题。
2. 修复“已提取角色”弹窗只显示已生成 Skill 的角色问题，现在优先显示首次识别出的全量候选角色。
3. 新增首次识别角色列表持久化文件 `recognized-characters.json`，避免后续重新提取时丢失未生成 Skill 的角色。
4. 已完成 Windows 便携版打包，产物位于 `release-portable/QMaiWrite.exe`。

## 20260620-2350

1. 修复拆书库角色 Skill 点击“加入自定义灵魂库”会按整本书批量导入的问题，现在只导入当前选中的角色 Skill。
2. 自定义灵魂库导入增加同作品同角色去重，重复点击不会再生成多个相同角色灵魂。
3. 已加入自定义灵魂库的角色按钮显示“已加入自定义灵魂库”并禁用，成功加入时显示当前角色的中文反馈。
4. 已完成 Windows 便携版打包，产物位于 `release-portable/QMaiWrite.exe`。

## 20260620-2248

1. 修复拆书库选择分析章节后，AI 已识别人物但因模型返回中文字段名导致角色列表为空、角色人物选择弹窗不弹出的问题。
2. 角色识别结果现在兼容“角色名、重要度、类别、章节索引、别名”等中文字段，并保留原有英文 JSON 字段兼容。
3. 新增角色识别单元测试，覆盖中文字段返回格式，防止同类问题回归。
4. 已完成 Windows 便携版打包，产物位于 `release-portable/QMaiWrite.exe`。
